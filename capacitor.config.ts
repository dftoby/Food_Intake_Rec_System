import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Food_Rec_Intake_System',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
